const BASE_URL = 'http://localhost:3000'

const obj1 = {
    size: 10,
    offset: 20,
    interviewDate: "14040425"
}


// complete createQueryStringFromObject function
const createQueryStringFromObject = (queryObj: Record<string, string | number>): string => {
    return ''
}

createQueryStringFromObject(obj1) // http://localhost:3000/api/users?size=10&offset=20&interviewDate=interviewDate